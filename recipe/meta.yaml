{% set name = "neptune-pytorch-lightning" %}
{% set version = "0.9.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/neptune-ai/{{ name }}/archive/{{ version }}.tar.gz
  sha256: f89f3dd69930b1783f19101361649cd302e5e6e3cbd90d3b5d014e8f2d9c559e

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6
    - neptune-client >=0.9.8
    - pytorch-lightning

test:
  imports:
    - neptune_pytorch_lightning.impl
  requires:
    - pip
  commands:
    - pip check

about:
  home: https://neptune.ai/
  license: Apache-2.0
  license_family: APACHE
  license_file: LICENSE
  summary: Neptune Client integration with PyTorch Lightning
  doc_url: https://docs.neptune.ai/integrations-and-supported-tools/model-training/pytorch-lightning
  dev_url: https://github.com/neptune-ai/neptune-pytorch-lightning

extra:
  recipe-maintainers:
    - aniezurawski
    - HubertJaworski
    - pitercl
    - PiotrJander
    - shnela
    - pkasprzyk
